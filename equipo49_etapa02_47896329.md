# Contribución individual -- Etapa 02
**Equipo:** 49
**Integrante:** Milton Nahuel Rodriguez Rivas 
**Fecha:** 2026-09-17

## 1. Aporte realizado
Describir brevemente las actividades realizadas durante esta etapa.
- El aporte que he realizado durante la etapa 2 consiste en desarrollar primeramente el DER (Diagrama Entidad Relacional)
- Agregué el esquema relacional normalizado (1FN → 2FN → 3FN) como imagen: `docs/etapa-02/ESQUEMA RELACIONAL ETAPA 2.png` (15 tablas: `PERSONA`, `CLIENTE`, `VENDEDOR`, `DIRECCION_CLIENTE`, `DIRECCION_PROVEEDOR`, `PROVEEDOR`, `MODELO_MOTOCICLETA`, `MOTOCICLETA`, `ACCESORIO`, `VENTA`, `DETALLE_VENTA_ACCESORIO`, `PROVISION_MOTOCICLETA`, `PROVISION_ACCESORIO`, `HISTORIAL_DE_PRECIO`, `COTIZACION`).
- Reestructuré la documentación de la etapa: renombré `docs/etapa-02/etapa-02.md` a `docs/etapa-02/ETAPA_02.md` y agregué encabezado de entrega/pregunta, tabla de material, índice de contenido, sección 7 (Diagramas y archivos, con imagen embebida y referencia al `.erdplus`) y sección 8 (Notas de diseño).
- Verifiqué y corregí la notación Chen contra el esquema relacional: eliminé `docs/etapa-02/Notacion chen- diagrama relacional.txt` y creé `docs/etapa-02/notacion_chen_RS.txt` como versión unificada/corregida.
- Reescribí el `README.md` general (equipo X Motors, tabla de integrantes, estado de etapas I–V, estructura del repositorio y nota de diseño vigente).

## 2. Decisiones en las que participé
Indicar decisiones relacionadas con el dominio, alcance o reglas de negocio.
- Congelar precios pactados en la venta (`VENTA` / `DETALLE_VENTA_ACCESORIO`) independientes de `MODELO_MOTOCICLETA.precio_lista`, con historial en `HISTORIAL_DE_PRECIO`.
- Composición de la venta: exactamente 1 motocicleta (por chasis) + 0..N accesorios opcionales.
- Modelar `PERSONA` como supertipo con especialización a `CLIENTE` / `VENDEDOR`, y `DIRECCION_CLIENTE` / `DIRECCION_PROVEEDOR` como especialización o entidades débiles.
- Representar `PROVISION_MOTOCICLETA` y `PROVISION_ACCESORIO` como tablas intermedias M:N (sin atributos propios) y `VENTA`–`ACCESORIO` como M:N con atributos propios (`cantidad`, `precio_unitario`) en el rombo.
- Dejar `VENTA.metodo_pago` como `VARCHAR` en Etapa II y documentar que la tabla normalizada `METODO_PAGO` se crea en Etapa III (nota de diseño en `README.md` y `ETAPA_02.md`).

## 3. Problemas o dificultades identificadas
Describir dificultades encontradas durante el desarrollo.
- Inconsistencias entre el DER textual anterior y el esquema relacional: nombres (`id_modelo` vs `id_modelo_motocicleta`, `cilindrada` vs `cc_cilindrada`, `numero_chasis`/`numero_motor` vs `nro_chasis`/`nro_motor`), atributo inexistente `precio_venta_pactado` en `VENTA`, `precio_pactado` en vez de `precio_unitario`, FKs duplicadas como atributos y cardinalidad incorrecta `MOTOCICLETA`–`VENTA` (1:1 total en ambos lados).
- Documentación poco navegable: `etapa-02.md` en minúsculas, sin tabla de material, sin índice, sin diagrama embebido ni referencia clara al `.erdplus` / `.txt` Chen. 


## 4. Soluciones o propuestas realizadas
Explicar brevemente cómo se resolvieron o propusieron resolver las dificultades.
- Crucé el `.erdplus` / PNG relacional con el `.txt` Chen y dejé una única versión verificada (`notacion_chen_RS.txt`): nombres exactos del relacional, notación IS-A explícita, débiles con `[[ ]]` y relación identificadora, sin FKs como atributos y cardinalidad corregida `MOTOCICLETA (0,1)` — `VENTA (1,1)`.
- Normalicé nombres y estructura de docs (`ETAPA_02.md` en mayúsculas, secciones de material/contenido/diagramas/notas) y dejé documentada la decisión de `metodo_pago` para no modificar las Etapas I–II.

## 5. Evidencias en el repositorio
Indicar los archivos, commits, issues o pull requests relacionados con el aporte.

- `docs/etapa-02/ESQUEMA RELACIONAL ETAPA 2.png` (nuevo, esquema 3FN)
- `docs/etapa-02/ETAPA_02.md` (renombrado desde `etapa-02.md` + secciones de material, contenido, diagramas y notas de diseño)
- `docs/etapa-02/notacion_chen_RS.txt` (nuevo, reemplaza a `Notacion chen- diagrama relacional.txt`)
- `README.md` (reescrito: equipo, estado de etapas, estructura, nota de diseño)
- `docs/etapa-01/ETAPA_01.md` (nuevo, lectura rápida de Etapa I; contextual)
- Commits: `a1abf61` ("add etapa 2 | esquema relacional normalizado 1fn -> 2fn -> 3fn") y `c4f0e95` ("add: READMEs ... y chequeo de notacion chen etapa 2")
- Pull Request: #5 (`feat/milton` → `main`, merge `9bf2db8`)


## 6. Reflexión individual
¿Qué concepto o competencia de Bases de Datos I considero que desarrollé en esta etapa?
- Desarrollé la competencia de identificación de datos en el mundo real y como ordenarlos de cierta manera para modelar un problema en sí a un sistema informático.
  En este caso, formalmente: pasaje del modelo conceptual (DER en notación Chen: entidades fuertes/débiles, ISA(generalización, especialización), cardinalidades) al modelo lógico relacional, y la justificación de la normalización 1FN → 2FN → 3FN (atomicidad, dependencia total de la clave y eliminación de dependencias transitivas (con precios congelados e historiales)).
